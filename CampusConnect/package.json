{
  "name": "campus-connect",
  "version": "1.0.0",
  "description": "Root package for Campus Connect to run both backend and frontend easily",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
